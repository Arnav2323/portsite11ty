{
  "name": "portsite11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npm run prebuild && npx @11ty/eleventy --serve",
    "prebuild": "rd /s /q _site && mkdir _site",
    "build": "npm run prebuild && eleventy",
    "dryrun": "eleventy --dryrun",
    "buildLinux": "mkdir _site && npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}