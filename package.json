{
  "name": "portsite11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx @11ty/eleventy --serve",
    "prebuild": "rd /s /q _site && mkdir _site",
    "build": "npx @11ty/eleventy",
    "dryrun": "eleventy --dryrun",
    "buildLinux": "npx @11ty/eleventy && npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}